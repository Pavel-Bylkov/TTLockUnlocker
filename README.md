# TTLockUnlocker — автоматизация управления замками TTLock через облако

> ⚠️ Важно: используйте только `python-telegram-bot` (строка `python-telegram-bot==20.7` в `requirements.txt`). Не добавляйте `telegram==0.0.1` — это приведёт к ошибкам импорта!

## Описание

Комплекс для автоматического управления замками TTLock через облачный API с уведомлениями в Telegram и на Email. Включает:
- `auto_unlocker.py` — сервис для автоматического открытия замка по расписанию и отправки уведомлений о критических сбоях на Email.
- `telegram_bot.py` — Telegram-бот для ручного управления замком, настройки расписания и параметров системы.
- **config.json** — файл конфигурации, хранит настройки расписания, часового пояса и перерывов.
- Гибкая настройка через `.env` и `config.json`.
- Логирование с ротацией, безопасность, работа в Docker.

## Архитектура

- **auto_unlocker**: основной сервис, открывает замок по расписанию, отправляет уведомления в Telegram, пишет логи и отправляет Email в случае критических сбоев.
- **telegram_bot**: отдельный контейнер, позволяет менять настройки через Telegram.
- **config.json**: общий файл с настройками, пробрасывается в оба контейнера.
- **.env**: должен лежать в папке `env/` (`env/.env`) и пробрасывается в контейнеры.
- **logs**: логи обоих сервисов доступны на хосте через bind-mount.

# Проект для автоматизации управления замками TTLock через Telegram-бота

## Установка и запуск

### 1. Обновление системы
- После подключения обновите пакеты:
  ```bash
  sudo apt update && sudo apt upgrade -y
  ```

### 2. Установка необходимых пакетов
- Установите Docker и Docker Compose:
  ```bash
  sudo apt install -y docker.io docker-compose
  ```

### 3. Добавление пользователя в группу Docker (опционально)
- Чтобы запускать Docker без `sudo`, добавьте своего пользователя в группу Docker:
  ```bash
  sudo usermod -aG docker $USER
  ```
- Выйдите и снова войдите в систему, чтобы изменения вступили в силу.

### 4. Клонирование проекта с GitHub
- Установите Git, если он еще не установлен:
  ```bash
  sudo apt install -y git
  ```
- Клонируйте репозиторий:
  ```bash
  git clone https://github.com/Pavel-Bylkov/TTLockUnlocker.git
  cd TTLockUnlocker
  ```

### 5. Настройка проекта
- Отредактируйте существующий файл `config.json` в соответствии с вашими требованиями.
- Скопируйте `.env` из примера ниже, заполните его и положите в папку `env/.env`:
```bash
   mkdir -p env
   chown -R 1000:1000 env
   chmod -R 755 env
   nano env/.env
   ```
- **Создайте папку для логов и выдайте права (обязательно!):**
   ```bash
   mkdir -p logs
   chown -R 1000:1000 logs
   chmod -R 755 logs
   ```
   (UID 1000 — стандартный для большинства Linux, если у вас другой UID для пользователя контейнера, используйте его)

### 6. Запуск проекта

**Запуск проекта через `make` (рекомендуется):**

Для первого запуска (сборка и запуск контейнеров):
```bash
make start
```
или
```bash
./start.sh
```

Для последующих запусков (если контейнеры уже собраны):
```bash
make up
```

Для остановки контейнеров:
```bash
make down
``` 

### 7. Проверка работы
- Убедитесь, что все контейнеры запущены и работают корректно:
  ```bash
  docker ps
  ```
- Проверьте логи:
  ```bash
  docker logs telegram_bot_1
  docker logs auto_unlocker_1
  ```

## Настройка переменных окружения (.env)

Файл `env/.env` используется для хранения всех секретных данных.

**Пример `.env`:**
```
# TTLock API
TTLOCK_CLIENT_ID=ваш_client_id_из_TTLock_Open_Platform
TTLOCK_CLIENT_SECRET=ваш_client_secret_из_TTLock_Open_Platform
TTLOCK_USERNAME=ваш_email_от_TTLock
TTLOCK_PASSWORD=ваш_пароль_от_TTLock
# TTLOCK_LOCK_ID=опционально_если_нужно_жёстко_задать

# Telegram
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
TELEGRAM_CHAT_ID=ваш_telegram_chat_id
TELEGRAM_CODEWORD=secretword
AUTO_UNLOCKER_CONTAINER=auto_unlocker_1
# ENV_PATH=/app/.env   # Не указывайте для первого запуска бота локально!

# Email (для уведомлений о критических сбоях)
SMTP_SERVER=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your_login@yandex.ru
SMTP_PASSWORD=your_app_password 
EMAIL_TO=recipient@example.com
```

- **Важно:** `TTLOCK_USERNAME` и `TTLOCK_PASSWORD` — только от аккаунта **владельца** замка!
- **`SMTP_PASSWORD`**: для большинства почтовых сервисов (Яндекс, Google, Mail.ru) требуется создать специальный "пароль для приложений". Не используйте ваш основной пароль от почты.
- Не публикуйте `.env` и логи с чувствительными данными.

## Возможности и управление через Telegram-бота

Бот предоставляет удобное меню с кнопками для управления всеми основными функциями. Для вызова меню отправьте команду `/start` или `/menu`.

### Основные команды
- **`📊 Статус`**: Показывает текущий статус расписания, часовой пояс и статус Docker-контейнера `auto_unlocker`.
- **`📅 Расписание`**: Открывает подменю для настройки расписания.
  - **`✅ Включить/❌ Выключить`**: Глобально включает или отключает автооткрытие по расписанию.
  - **`⏰ Настроить время`**: Установка времени открытия для каждого дня недели.
  - **`🕒 Настроить перерывы`**: Добавление/удаление "перерывов", во время которых замок не будет открываться.
  - **`🌍 Настроить часовой пояс`**: Установка часового пояса (например, `Europe/Moscow`).
- **`🔓 Открыть / 🔒 Закрыть`**: Мгновенно отправляет команду на открытие или закрытие замка.
- **`⚙️ Настройки`**: Дополнительные настройки системы.
  - **`👤 Сменить получателя`**: Запускает процедуру смены `TELEGRAM_CHAT_ID` через кодовое слово.
  - **`✉️ Установить Email`**: Устанавливает или меняет email для получения уведомлений о критических ошибках.
  - **`🧪 Тест Email`**: Отправляет тестовое письмо для проверки настроек SMTP.
- **`📝 Логи`**: Показывает последние 10 строк из лога `auto_unlocker.py`.
- **`🔄 Перезапуск`**: Перезапускает контейнер `auto_unlocker_1`.

### Email-уведомления
- Сервис `auto_unlocker` автоматически отправит email на адрес, указанный в `EMAIL_TO`, если не сможет открыть замок после нескольких попыток.
- Это позволяет оперативно узнать о серьезных проблемах (например, если шлюз не в сети).

### Смена получателя уведомлений
1. В Telegram нажмите `⚙️ Настройки` -> `👤 Сменить получателя`.
2. Введите кодовое слово (указанное в `.env`).
3. Подтвердите смену (ответьте "да").
4. Бот обновит `.env` и автоматически перезапустит сервис `auto_unlocker`.

## Запуск тестов

Для запуска всех юнит-тестов используйте:
```
make test
```
или
```
pytest
```
Для получения отчета о покрытии тестами:
```
pytest --cov=.
```

